# 호환성 및 검증 범위

## 지원 원본

| 항목 | 값 |
| --- | --- |
| 플랫폼 | Game Boy Advance |
| 지역 | 일본 |
| 게임 코드 | `BSKJ` |
| 원본 크기 | `16,777,216 bytes` |
| 원본 SHA-256 | `9abbff51004531eb0f9ee4c15af5b53db7ffe5e9c47e1e225488c576db64360f` |

파일명이 같아도 SHA-256이 다르면 지원 대상이 아닙니다. 트림, 헤더 추가, 다른 리비전, 기존 패치가 적용된 ROM에는 적용하지 마십시오.

## 적용 결과

| 항목 | 값 |
| --- | --- |
| 결과 크기 | `33,554,432 bytes` |
| 결과 SHA-256 | `1c057a8e65676ec38e9cbd1ee6b5c60eab48ba6bd6f456d7574ae71b289a2b14` |
| 패치 형식 | xdelta3/VCDIFF, 애플리케이션 헤더 없음 |

## 검증 완료

- xdelta 생성 및 역적용 바이트 일치
- 번역 대상 텍스트 42,661개 빌드 반영
- 키보드용 원문 행 21개를 런타임 한글 입력 모듈로 대체
- 이미지 결정 120/120 반영, 한국어 이미지 116건과 원본 유지 4건 확인
- 현대 한글 완성형 11,172자와 한글 자모 40자
- 코드베이스에서 `임라이언` 조합·삭제·복원과 11,212개 토큰 왕복 검증
- mGBA에서 부팅, 새 게임, 이름 `임라이언` 입력·확정 후 다음 장면 진입 확인
- 게임 시작 구간 진행, 저장 완료, 재부팅, `이어하기`, 저장 슬롯 불러오기와 공방 복귀 확인
- 타이틀과 시작·저장·불러오기 경로에서 한글 텍스트 및 이미지 표시 확인
- `환경 설정` 메뉴 표기는 한국어로 유지하고, 진입 화면은 타일 깨짐 방지를 위해 일본판 원본 `CONFIG` 그래픽 유지
- 소유자 직접 이미지 검수와 최종 배포 승인

## 공개된 한계

- 모든 분기와 엔딩을 처음부터 끝까지 자동 순회한 장기 런타임 검증은 아닙니다.
- 실제 GBA 하드웨어 검증은 수행하지 않았습니다.
- 런타임 검증 범위는 부팅부터 이름 입력, 초반 진행, 저장과 불러오기까지입니다.
- 에뮬레이터 필터, 비정수 배율, 셰이더에 따라 픽셀 글꼴 인상이 달라질 수 있습니다.
- 원본과 해시가 다른 덤프는 지원하지 않습니다.
